// indexOf takes a substring and checks if it exists in a string ,
// it returns the first position of the substring if not it returns -1

// let hobby = 'running round the stadium'
console.log(hobby.indexOf('s'))