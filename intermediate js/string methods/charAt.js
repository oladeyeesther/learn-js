//char_at takes the index and returns the value at that index

// string.charAt(index)
let lesson = "30daysofjavascript"
console.log(lesson.charAt(0))
lastIndex=lesson.length -1
console.log(lesson.charAt(lastIndex))