//includes.js takes a substring srgument and it checks if the substring argument exists
// in the string. includes() return a boolean.
// it checks if a substring exist in a string and it returns true if it existsand false if it doesn't

let hobby = 'running round the stadium everyday'
console.log(hobby.includes('round'))
console.log(hobby.includes('Stadium'))