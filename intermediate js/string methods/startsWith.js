//starstWith takes a substring as an argument and it checks 
// if the string starts with that specified substring.
// it returns a boolean (true or false)
// string.startsWith(substring)

let book = 'nneka the village girl'
console.log(book.startsWith('nneka'))
console.log(book.startsWith('village'))