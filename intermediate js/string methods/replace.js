//replace takes in two parameters the old substring and new subring
// string.replace(oldsubstring, newsubstring)

let program = 'intermediate python'
console.log(program.replace('python', 'javascript'))
let state = 'delta'
console.log(state.replace('ta', 'za'))