// concat takes any substrings and creates concatenation.
// string.concat(substring, substring, substring)

let course = 'javascript'
console.log(course.concat(" fundamental "))
let country = " Wonder"
console.log(country.concat(" land "))
